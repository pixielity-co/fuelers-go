{
  "name": "@packages/logger",
  "version": "1.0.0",
  "private": true,
  "description": "logger package",
  "main": "src/logger.go",
  "scripts": {
    "setup": "echo 'Setting up logger package...'",
    "dev": "air",
    "build": "go build ./src/... && mkdir -p dist && touch dist/.built",
    "test": "go test -v ./src/...",
    "lint": "golangci-lint run ./src/...",
    "clean": "rm -rf dist coverage.out",
    "typecheck": "go vet ./src/...",
    "tidy": "go mod tidy",
    "format": "go fmt ./src/..."
  },
  "dependencies": {},
  "devDependencies": {},
  "peerDependencies": {}
}
